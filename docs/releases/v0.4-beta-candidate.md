# v0.4 Beta Candidate - Kubernetes-Native Read-Only Operator Foundation

Status: beta-candidate release note for Phase 35 and Phase 36.

## Summary

Seaweed Block v0.4 beta candidate adds Kubernetes-native read-only visibility
and actionable status on top of the Helm/PVC/read-only-ops path:

```text
SwBlockCluster CRD
SwBlockVolume CRD
status-only operator-status controller
Kubernetes Conditions
Kubernetes Events
node readiness status
support evidence refs
cleanup visibility
safe next-step hints
status/events-only RBAC
```

This is the first operator foundation slice plus its read-only actionability
layer. It is not the full operator lifecycle and it does not add mutating admin
actions.

## User-Visible Improvements

- `SwBlockCluster` and `SwBlockVolume` CRDs provide normal Kubernetes objects
  for status inspection.
- The operator-status controller can publish existing ManagedVolume facts into
  CRD `.status`.
- `Ready`, `Blocked`, `Recovered`, `Recovering`, `CleanupRequired`, and
  `EvidenceStale` are part of the Kubernetes Condition vocabulary.
- Known blocked states such as `csi_node_image_pull_failed` publish
  `Ready=False` and `Blocked=True`, not false readiness.
- Stale or unreachable evidence publishes `Ready=Unknown` and
  `EvidenceStale=True`, not an inappropriate hard block.
- SmartWAL integrity faults publish non-ready status with
  `reason=wal_integrity_fault`.
- Kubernetes Events are emitted with stable object/type/reason identity so a
  persistent condition does not create an unbounded stream of new Event objects.
- `SwBlockCluster.status.nodes[]` exposes Kubernetes node readiness and
  Seaweed Block node-preflight judgments where evidence is available.
- Blocked or unknown status can point to support-bundle evidence refs and safe
  read-only/scripted next steps.
- Cleanup verifier evidence projects `CleanupRequired=True|False`, residue
  counters, and safe scripted verification hints without performing cleanup.
- CRD status, Events, report, dashboard, operator-snapshot, and `ops explain`
  agree across healthy, blocked, stale, cleanup-required, and multi-volume
  paths.
- The operator-status ServiceAccount is constrained to CRD read, CRD `/status`
  write, and Event create.

## Validated Evidence

| Gate | Evidence | Result |
|---|---|---|
| D1 CRD/RBAC contract | `phase35-d1-operator-crd-qa-signoff.md` | PASS |
| D2 dry-run packaged controller | `phase35-d2-operator-status-qa-signoff.md` | PASS |
| D3 happy-path status writer | `phase35-d3-operator-status-writer-qa-signoff.md` | PASS |
| D4 blocked status + Events | `phase35-d4-operator-status-events-qa-signoff.md` | PASS |
| D5 stale/unreachable projections | `phase35-d5-stale-status-projections-qa-signoff.md` | PASS |
| D6 stable Event identity | `phase35-d6-stable-event-identity-qa-signoff.md` | PASS |
| D7 read-only boundary | `phase35-d7-read-only-boundary-qa-signoff.md` | PASS |
| Phase 36 D2 node readiness | `phase36-d2-node-readiness-qa-signoff.md` | PASS |
| Phase 36 D3 support evidence refs | `phase36-d3-support-evidence-qa-signoff.md` | PASS |
| Phase 36 D4 cleanup visibility | `phase36-d4-cleanup-visibility-qa-signoff.md` | PASS |
| Phase 36 D5 surface agreement | `phase36-d5-surface-agreement-qa-signoff.md` | PASS |

Final Phase 35 close:

- `internal/docs/finished-plans/phase35_finishedplan_kubernetes_native_read_only_operator_foundation.md`

Final Phase 36 close:

- `internal/docs/finished-plans/phase36_finishedplan_productized_operations_actionability.md`

## Current Claim

Seaweed Block v0.4 beta candidate may claim:

- CRDs for `SwBlockCluster` and `SwBlockVolume` exist with status
  subresources.
- The optional operator-status Deployment can publish read-only status into
  existing CR objects.
- Kubernetes Conditions and Events mirror the existing `sw-block ops`
  ManagedVolume vocabulary.
- Node readiness, support evidence references, cleanup visibility, and safe
  next-step hints are visible through read-only status surfaces.
- Negative-first behavior is preserved:
  - blocked evidence becomes `Ready=False` / `Blocked=True`,
  - stale or unreachable evidence becomes `Ready=Unknown` /
    `EvidenceStale=True`,
  - known WAL integrity faults never become `Ready=True`.
- The operator-status ServiceAccount cannot mutate storage, workloads, PVCs,
  PVs, Secrets, StorageClasses, CRD spec, Helm releases, iSCSI sessions,
  multipath maps, or hostPath data.

## Explicit Non-Claims

- Not production-ready.
- No mutating Kubernetes operator lifecycle.
- No automatic `SwBlockVolume` object creation or ownership.
- No finalizers or delete safety.
- No automatic cleanup.
- No automatic support-bundle collection or upload.
- No promote, repair, rebuild, failback, delete, backup, restore, or cleanup
  mutation through UI/API/operator.
- No upgrade execution.
- No backup/snapshot/restore workflow.
- No returned-replica rebuild or automated failback.
- No NVMe ANA parity expansion.
- No production hosted dashboard.
- No performance, RTO, RPO, or SLO claim.

## Known Follow-Ups

- Add server-side dry-run or envtest validation for CRD status payloads so
  future schema drift is caught before live QA.
- Populate live node evidence from real Kubernetes node readiness, image
  presence, and CSI driver registration instead of relying on replay-only
  missing-image evidence.
- Tighten local-image build/import evidence so the build host's k3s proves both
  `sw-block:local` and `sw-block-csi:local` are present before live local-image
  gates claim node readiness.
- Document loopback publish targets as single-node/local-consumer only.
- Keep cleanup verifier strict for force-delete residue such as stale iSCSI node
  DB records.
- Add controller ownership for CR object creation only after the read-only
  status contract remains stable.
